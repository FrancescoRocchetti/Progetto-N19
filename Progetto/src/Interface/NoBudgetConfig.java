package Interface;

import javax.swing.*;

public class NoBudgetConfig {
    public NoBudgetConfig(){
        /*
        VEDI PACKAGE Automatic

        * Logica di funzionamento (base):
        * Per ogni componente, scelgo quello con ranking max && prezzo max
        * */
    }
}
